{% extends 'base.html.twig' %}

{% block body %}
    Category: {{ category.name }}<br>
    Page: {{ page }}<br>

    <div class="row">
        {% include 'partials/products.html.twig' with {'products': category.products} %}
    </div>
{% endblock %}
